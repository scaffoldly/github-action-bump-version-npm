{
  "name": "bump-version-action",
  "version": "1.0.0",
  "description": "Scaffoldly Bump Version Action",
  "main": "index.js",
  "repository": "git@github.com:scaffoldly/bump-version-action.git",
  "author": "Christian Nuss",
  "license": "GPL-3.0-or-later",
  "scripts": {
    "build": "ncc build index.js --license licenses.txt",
    "prepare": "yarn build",
    "version": "git add -A dist",
    "postversion": "git push origin HEAD:main && git push --follow-tags"
  },
  "dependencies": {
    "@actions/core": "^1.2.6",
    "@actions/github": "^4.0.0",
    "actions-toolkit": "^6.0.1"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.27.0"
  }
}
